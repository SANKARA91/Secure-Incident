{
  "name": "secure-incident-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo 'No tests yet' && exit 0",
    "lint": "echo 'No lint yet' && exit 0",
    "build": "echo 'No build yet' && exit 0"
  }
}
